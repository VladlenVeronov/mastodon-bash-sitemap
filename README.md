# mastodon-bash-sitemap
Mastodon Bash Sitemap — a lightweight, high-performance sitemap generator. Uses direct SQL queries via Bash instead of heavy Ruby tasks. Perfect for Docker/Coolify setups. Features auto-container discovery, configurable limits for profiles/posts, and zero overhead. MIT licensed.
